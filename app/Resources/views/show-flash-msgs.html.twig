{% set modes = ['success', 'error', 'warning', 'info'] %}
{% for mode in modes %}
    {% for flashMessage in app.session.flashbag.get('fos_user_success') %}
        <!--  TODO: fix flash msgs -->
        <div class="alert alert-success">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            {{ flashMessage }}
        </div>
    {% endfor %}
{% endfor %}